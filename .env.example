# Backend API base URL. Required.
VITE_API_URL=https://api.sportfinding.com

# Where the dashboard sends users to sign in (the landing app's login page).
# Dev defaults to http://localhost:3000/login if unset.
VITE_LANDING_LOGIN_URL=https://app.sportfinding.com/login
